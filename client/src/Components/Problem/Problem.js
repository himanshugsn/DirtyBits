import React from "react";
import Menu from "./Menu";

function Problem() {
  const id = 1;
  const uid = 1;

  return (
    <div>
      <Menu id={id} uid={uid} />
    </div>
  );
}

export default Problem;
